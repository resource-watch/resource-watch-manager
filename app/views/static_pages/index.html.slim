#pageContent.c-page
  .row
    .column.small-12
      h1.c-title.-huge Static Pages

      .c-button-container.-end
        ul
          li
            = link_to 'New +', new_static_page_path, class: 'c-button'
      .c-table
        table.table
          thead
            tr
              th Title
              th Summary
              th Published
              th Actions
          tbody
            - for page in @static_pages
              tr
                td= page.title
                td= page.summary
                td= page.published
                td.individual-actions
                  ul
                    li= link_to 'Edit', edit_static_page_path(page)
                    li= link_to 'Remove', static_page_path(page), method: :delete,
                            data: { confirm: 'Are you certain you want to delete this page?' }
